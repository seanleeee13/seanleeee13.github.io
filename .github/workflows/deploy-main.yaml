name: Deploy Workspace [main] to GitHub Pages
on:
  push:
    branches:
      - main
    paths:
      - "main/**"
      - "pnpm-lock.yaml"
      - ".github/workflows/deploy-main.yaml"
      - "apply-change"
permissions:
  contents: write
  pages: write
  id-token: write
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      # - name: Run Linter
      #   run: pnpm --filter main lint
      - name: Build Project
        run: pnpm --filter main build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./main/dist"
      - name: Deploy Main
        uses: s0/git-publish-subdir-action@v2.6.0
        env:
          REPO: self
          BRANCH: gh-pages
          FOLDER: main/dist
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CLEAR_GLOBS: "assets,index.html"