name: Deploy Workspace [stage] to GitHub Pages
on:
  push:
    branches:
      - main
    paths:
      - "stage/**"
      - "pnpm-lock.yaml"
      - ".github/workflows/deploy-stage.yaml"
permissions:
  contents: write
  pages: write
  id-token: write
concurrency:
  group: "pages-stage"
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      # - name: Run Linter
      #   run: pnpm --filter stage lint
      - name: Build Project
        run: pnpm --filter stage build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./stage/dist"
      - name: Deploy Website
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./main/dist
          publish_branch: gh-pages
          destination_dir: stage
          force_orphan: true