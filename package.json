{
    "name": "seanleeee13-github-pages",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "yarn concurrently \"cd main && yarn dev\" \"cd gff && yarn dev\" \"cd something && yarn dev\""
    },
    "devDependencies": {
        "concurrently": "^10.8.0"
    }
}